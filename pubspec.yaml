name: reader_app
description: "A premium Flutter e-book reader."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI & 动画
  cupertino_icons: ^1.0.6
  flutter_animate: ^4.5.0        # 增强动画效果
  google_fonts: ^6.1.0           # (可选) 更好的字体支持

  # 状态管理
  flutter_riverpod: ^2.5.1

  # 数据库
  isar_community: ^3.3.0
  isar_community_flutter_libs: ^3.3.0

  # 文件与路径
  path_provider: ^2.1.2
  file_picker: ^8.0.0
  path: ^1.9.0
  permission_handler: ^11.3.0    # 权限处理

  # 阅读器核心
  epub_view: ^3.2.0
  flutter_pdfview: ^1.3.2

  # 工具
  http: ^1.2.0                   # AI 请求
  image: ^3.3.0                  # 封面处理
  epubx: ^4.0.0                  # EPUB 解析辅助

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.8
  isar_community_generator: ^3.3.0

  # ... (上面的 dependencies 等内容保持不变)

  # 【重点修复】确保文件末尾有这一段
flutter:
  uses-material-design: true  # 👈 这行代码找回图标